{
    "name": "flowc",
    "version": "0.1.0",
    "description": "Flow language compiler",
    "scripts": {
        "compile": "bun run compiler/main.ts",
        "build": "bun build compiler/main.ts --compile --outfile flowc.exe",
        "test": "powershell -File test.ps1"
    },
    "devDependencies": {
        "@types/bun": "^1.3.9"
    }
}
