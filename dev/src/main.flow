// dev — sandbox per provare modifiche a flow/, flow-os/, runtime/
// Esegui da qui: flow dev
// Usa runtime/ e compiler/ dalla repo (modifiche subito visibili)

fn main() {
    print("Ciao da dev")
}

@client
component App() {
    count = 0

    return Column {
     
        Text {
            value: "Benssskllssaaaassasssddsasaskklop"
            style: style({ size: 40, weight: "bold", color: "#111" })
        }

        Button {
            text: "C: {count}"
            
            style: style({
                bg:     "#3b82f6"
                color:  "#fff"
                radius: 8
                padding: 12
                hover:  { bg: "#2563eb" }
            })
        }
    }
}
