// Esempio Flow — server + client

fn saluta(nome: str) {
    print("Ciao " + nome)
}

fn main() {
    saluta("Mario")
    print("Flow funziona")
}

@client
component Home() {
    return Column {
        Text { value: "Benvenuto in Flow", style: { size: 28, weight: "bold", color: "#111" } }
        Button { text: "Inizia", style: { bg: "#3b82f6", color: "#fff", radius: 8, padding: 12 } }
    }
}
